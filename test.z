imp "iostream"
imp "stdio.h"

bool something(num a):
    for (num i <- 0; i < a; i++):
        out i
        out '\n'
    end
    ret (true)
end

main:
    num a
    bool b
    a <- 5
    b <- something(a)
    out b
end